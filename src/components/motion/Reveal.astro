---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  direction?: 'up' | 'down' | 'left' | 'right';
  delay?: number;
  class?: string;
}

const { as = 'div', direction = 'up', delay, class: className = '' } = Astro.props as Props;
const Tag = as;
---

<Tag class={className} data-reveal={direction} data-reveal-delay={delay}>
  <slot />
</Tag>
